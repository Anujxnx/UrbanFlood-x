'use strict';

var vector3 = require('./vector3.cjs');

function vec3(x, y, z) {
  if (Array.isArray(x)) return new vector3.default(x[0], x[1], x[2]);
  return new vector3.default(x, y, z);
}

exports.Vector3 = vector3.default;
exports.vec3 = vec3;
