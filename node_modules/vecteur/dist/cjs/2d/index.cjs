'use strict';

var vector2 = require('./vector2.cjs');

function vec2(x, y) {
  if (Array.isArray(x)) return new vector2.default(x[0], x[1]);
  return new vector2.default(x, y);
}

exports.Vector2 = vector2.default;
exports.vec2 = vec2;
