const randomInRange = (min, max) => Math.random() * (max - min) + min;
const lerp = (a, b, n) => (1 - n) * a + n * b;
const clamp = (n, min, max) => Math.max(Math.min(n, max), min);

export { clamp, lerp, randomInRange };
